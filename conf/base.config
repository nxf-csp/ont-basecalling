/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    nxf_ont/basecalling Nextflow base config file
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    A 'blank slate' config file, appropriate for general use on most high performance
    compute environments. Assumes that all software is installed and available on
    the PATH. Runs in `local` mode - all jobs will be run on the logged in environment.
----------------------------------------------------------------------------------------
*/

process {

    cpus   = { Runtime.runtime.availableProcessors() }
    memory = { 256.GB   * task.attempt }
    time   = null

    errorStrategy = { 
        if (task.exitStatus in ((130..145) + 71 + 104 + 175)) {
            return 'retry'
        } else if (task.exitStatus == -1 || task.exitStatus == null) {
            // Вероятно зависание - retry с увеличенными ресурсами
            return 'retry' 
        } else if (task.attempt <= 3) {
            return 'retry'
        } else {
            return 'finish'  // Останавливаем после 3 попыток
        }
    }
    maxRetries    = 3
    maxErrors     = '-1'

    // Process-specific resource requirements
    // NOTE - Please try and reuse the labels below as much as possible.
    //        These labels are used and recognised by default in DSL2 files hosted on nf-core/modules.
    //        If possible, it would be nice to keep the same label naming convention when
    //        adding in your local modules too.
    // See https://www.nextflow.io/docs/latest/config.html#config-process-selectors
    withLabel:process_single {
        cpus   = { 1                   }
        memory = { 6.GB * task.attempt }
        time   = { 4.h  * task.attempt }
    }
    withLabel:process_low {
        cpus   = 1
        memory = 4.GB
        time   = 20.m
    }
    withLabel:process_medium {
        cpus   = { 32     * task.attempt }
        memory = { 32.GB * task.attempt }
        time   = { 30.m   * task.attempt }
    }
    withLabel:process_high {
        cpus   = { 128    * task.attempt }
        memory = { 72.GB * task.attempt }
    }
    withLabel:process_long {
        time   = { 20.h  * task.attempt }
    }
    withLabel:process_high_memory {
        memory = { 200.GB * task.attempt }
    }
    withLabel:error_ignore {
        errorStrategy = 'ignore'
    }
    withLabel:error_retry {
        errorStrategy = 'retry'
        maxRetries    = 2
    }
    withLabel: gpu_intensive_task {
        ext.use_gpu = true
        // Количество GPU (если не указаны конкретные ID)
        accelerator = (System.getenv("GPUS_FOR_BASECALLING") ?: "1").toInteger()
        // Конкретные ID GPU (приоритет выше)
        ext.gpu_ids = System.getenv("GPU_IDS_FOR_BASECALLING") ?: null
        containerOptions = {task.ext.gpu_ids ? 
            "--gpus device=${task.ext.gpu_ids}" : 
            "--gpus ${task.accelerator.request}"}

        time   = { 20.h   * task.attempt }
    }
}
